[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gads-cli"
dynamic = ["version"]
description = "Unified CLI for Google Ads, Business Profile, Merchant Center, and GA4"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
keywords = ["google-ads", "google-business-profile", "merchant-center", "ga4", "cli", "claude-code"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Office/Business",
]
dependencies = [
    "click>=8.5.0",
    "requests>=2.34.2",
    "google-auth>=2.57.1",
    "google-auth-oauthlib>=1.4.1",
    "python-dotenv>=1.2.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

[project.scripts]
gads = "gads_lib.cli:main"

[project.urls]
Homepage = "https://github.com/talas9/gads-cli"
Repository = "https://github.com/talas9/gads-cli"
Issues = "https://github.com/talas9/gads-cli/issues"

[tool.setuptools.packages.find]
include = ["gads_lib*"]

[tool.setuptools.dynamic]
version = {attr = "gads_lib.__version__"}
